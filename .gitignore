.DS_Store
**/.DS_Store
*.swp
*.swo
.idea/
.vscode/
node_modules/

# Local working dirs (not deployed)
# bayshore-output/ — /bayshore-productize skill output (recs, scopes, drafts)
# .playwright-mcp/ — Playwright verification snapshots/screenshots
bayshore-output/
.playwright-mcp/

# Analytics reports (private — repo is public).
# No trailing slash: the real files live in ~/.local/share/bayshore-analytics
# (outside macOS TCC so launchd crons can read them) and `reports` here is a symlink.
reports
